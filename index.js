/* ========================================================================= */
const pause = async (ms) => await new Promise(r=>setTimeout(r, ms));

/* ========================================================================= */
exports.pause = pause;